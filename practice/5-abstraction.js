class Car {
    constructor(brand, model) {
        this._brand = brand
        this._model = model
    }

    // getter

    get brand() {
        return this._brand;
    }
    get model() {
        return this._model;
    }
}

// unfinished code (mentornya lupa)